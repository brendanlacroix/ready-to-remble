{
  "name": "ready-to-remble",
  "version": "1.0.0",
  "homepage": "https://brendanlacroix.github.io/ready-to-remble/",
  "dependencies": {
    "classnames": "^2.2.6",
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-scripts": "2.0.3",
    "react-syntax-highlighter": "^8.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">5%",
    "last 2 versions"
  ],
  "devDependencies": {
    "gh-pages": "^2.0.0"
  }
}
